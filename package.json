{
  "name": "gmail-inbox-labels",
  "version": "1.2.0",
  "description": "Chrome extension that modifies Gmail label buttons to show only emails in the inbox",
  "main": "content.js",
  "scripts": {
    "build": "echo 'Extension built successfully'",
    "package": "mkdir -p dist && cp manifest.json content.js styles.css dist/ && cp -r images dist/",
    "zip": "npm run package && cd dist && zip -r ../gmail-inbox-labels.zip . && cd ..",
    "clean": "rm -rf dist temp-extension gmail-inbox-labels.crx gmail-inbox-labels.zip"
  },
  "keywords": [
    "chrome-extension",
    "gmail",
    "inbox",
    "labels"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "crx": "^5.0.1"
  }
}
